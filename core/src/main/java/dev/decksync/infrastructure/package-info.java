/**
 * Adapters for filesystem, Ludusavi parser, HTTP client, Steam locators, and platform resolvers.
 * Implements ports declared in {@link dev.decksync.application}.
 */
package dev.decksync.infrastructure;
