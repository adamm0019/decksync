# Don't fail rules whose `that()` clause matches zero classes.
# The domain / application / infra layers are empty in early M2 commits;
# without this flag every ArchUnit rule would fail until a class lands.
# Rules continue to enforce violations as soon as classes exist.
archRule.failOnEmptyShould=false
